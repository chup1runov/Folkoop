{
  "name": "sverinav",
  "version": "0.11.0",
  "private": true,
  "description": "Sverinav — Samhället. Enklare.",
  "scripts": {
    "start": "serve -s . -l $PORT",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
