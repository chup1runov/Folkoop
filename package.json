{
  "name": "sverinav",
  "version": "0.13.0",
  "private": true,
  "description": "Sverinav — Samhället. Enklare.",
  "scripts": {
    "build": "node scripts/build-site.mjs",
    "start": "node scripts/build-site.mjs && serve -s _site -l ${PORT:-3000}",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {"serve": "^14.2.4"}
}
